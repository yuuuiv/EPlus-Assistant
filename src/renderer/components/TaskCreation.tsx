import { ListTodo } from "lucide-react";
import type { Account, CreateTaskInputV2, EventOption, EventSnapshot, LotteryPreference } from "../../shared/ipc.js";
import { PerAccountReview, PreviewDigest, TaskPolicyControls, type TaskDraft } from "./TaskWorkflow.js";

export interface TaskForm { eventSnapshotId: string; accountIds: string[]; preference: LotteryPreference; perAccountSerialCodes: string; confirmationPolicy: CreateTaskInputV2["confirmationPolicy"]; riskAcknowledged: boolean; }
interface TaskCreationProps { readonly events: readonly EventSnapshot[]; readonly accounts: readonly Account[]; readonly form: TaskForm; readonly selectedEvent: EventSnapshot | undefined; readonly onFormChange: (form: TaskForm) => void; readonly onCreate: () => void; }

function optionFor(event: EventSnapshot | undefined, kind: EventOption["kind"]): EventOption | undefined { return event?.rawFormSchema.options.find((option) => option.kind === kind); }

export function TaskCreation(props: TaskCreationProps) {
  const ticket = optionFor(props.selectedEvent, "ticket"); const quantity = optionFor(props.selectedEvent, "quantity"); const payment = optionFor(props.selectedEvent, "payment"); const serialCode = props.selectedEvent?.rawFormSchema.serialCode;
  const firstEntry = props.form.preference.entries[0] ?? { rank: 1, ticketTypeId: "", quantity: 1 };
  const changePreference = (preference: LotteryPreference) => props.onFormChange({ ...props.form, preference });
  const taskDraft: TaskDraft = { accountIds: props.form.accountIds, confirmationPolicy: props.form.confirmationPolicy, preference: props.form.preference, riskAcknowledged: props.form.riskAcknowledged };
  const changeDraft = (draft: TaskDraft) => props.onFormChange({ ...props.form, ...draft });
  return <section className="workspace-panel" aria-labelledby="task-title"><div className="workspace-heading"><div><p className="section-kicker">演出与任务</p><h1 id="task-title">创建任务</h1><p>选择演出、偏好和账号。创建后须按策略确认才会进入队列。</p></div><span className="badge badge-yellow">{props.form.accountIds.length} 个账号待审阅</span></div><section className="panel-card"><div className="panel-head"><h2><ListTodo size={16} />任务偏好</h2><span className="muted">每个账号的实际偏好可在下方单独核对</span></div><div className="form-grid"><label className="full">演出快照<select value={props.form.eventSnapshotId} onChange={(event) => props.onFormChange({ ...props.form, eventSnapshotId: event.target.value })}>{props.events.map((event) => <option key={event.id} value={event.id}>{event.title}</option>)}</select></label><label>申请入口{ticket ? <select value={props.form.preference.applicationLinkId || firstEntry.ticketTypeId} onChange={(event) => changePreference({ ...props.form.preference, applicationLinkId: event.target.value, entries: [{ ...firstEntry, ticketTypeId: event.target.value }] })}><option value="">选择解析出的申请入口</option>{ticket.values.map((item) => <option key={item.id} value={item.id} disabled={item.disabled}>{item.label}</option>)}</select> : <input value={firstEntry.ticketTypeId} placeholder="申请入口标识" onChange={(event) => changePreference({ ...props.form.preference, entries: [{ ...firstEntry, ticketTypeId: event.target.value }] })} />}</label><label>票数{quantity ? <select value={String(firstEntry.quantity)} onChange={(event) => changePreference({ ...props.form.preference, entries: [{ ...firstEntry, quantity: Number(event.target.value) }] })}>{quantity.values.map((item) => <option key={item.id} value={item.id}>{item.label}</option>)}</select> : <input type="number" min="1" value={firstEntry.quantity} onChange={(event) => changePreference({ ...props.form.preference, entries: [{ ...firstEntry, quantity: Number(event.target.value) }] })} />}</label><label>付款方式{payment ? <select value={props.form.preference.paymentMethodId} onChange={(event) => changePreference({ ...props.form.preference, paymentMethodId: event.target.value })}><option value="">选择解析出的付款方式</option>{payment.values.map((item) => <option key={item.id} value={item.id}>{item.label}</option>)}</select> : <input value={props.form.preference.paymentMethodId} placeholder="付款方式标识" onChange={(event) => changePreference({ ...props.form.preference, paymentMethodId: event.target.value })} />}</label>{serialCode?.required ? <><label>公共抽选码<input value={props.form.preference.serialCode ?? ""} placeholder={serialCode.placeholder ?? "输入抽选码"} onChange={(event) => changePreference({ ...props.form.preference, serialCode: event.target.value })} /></label><label className="full">账号专用抽选码<textarea rows={4} value={props.form.perAccountSerialCodes} placeholder="邮箱=抽选码" onChange={(event) => props.onFormChange({ ...props.form, perAccountSerialCodes: event.target.value })} /></label></> : null}<label className="full">参与账号<select multiple value={props.form.accountIds} onChange={(event) => props.onFormChange({ ...props.form, accountIds: Array.from(event.currentTarget.selectedOptions).map((item) => item.value) })}>{props.accounts.map((account) => <option key={account.id} value={account.id}>{account.label || account.eplusEmail}</option>)}</select></label></div><TaskPolicyControls automated={true} policy={props.form.confirmationPolicy} acknowledged={props.form.riskAcknowledged} onPolicyChange={(confirmationPolicy) => props.onFormChange({ ...props.form, confirmationPolicy })} onAcknowledgedChange={(riskAcknowledged) => props.onFormChange({ ...props.form, riskAcknowledged })} /><div className="panel-head subsection-head"><h3>每账号偏好预览</h3><span className="muted">创建前确认每个账号的场次与抽选码</span></div><PerAccountReview accounts={props.accounts} event={props.selectedEvent} draft={taskDraft} onDraftChange={changeDraft} /><PreviewDigest event={props.selectedEvent} draft={taskDraft} accounts={props.accounts} /><div className="actions"><button className="primary" disabled={!props.form.riskAcknowledged || props.form.accountIds.length === 0} onClick={props.onCreate}><ListTodo size={16} />创建任务</button></div></section></section>;
}
